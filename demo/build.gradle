apply plugin: 'android'

dependencies {
    compile project(':library')
}

android {
    compileSdkVersion 14
    buildToolsVersion "19.0.1"

    defaultConfig {
        minSdkVersion 14
    }

    buildTypes {
        release {
            runProguard false
        }
    }
}
